# 《詩經》線上讀本：內容編輯指南

## 一、內容模型

本站把資料與介面完全分離。全部 305 篇原文位於 `client/src/data/shijing.ts`，每篇是一個 `Poem` 物件：

```ts
{
  id: 1,
  title: "關雎",
  chapter: "國風",
  section: "周南",
  stanzas: [
    "關關雎鳩，在河之洲。窈窕淑女，君子好逑。",
    "……"
  ]
}
```

`chapter` 只使用六個固定值：`國風`、`小雅`、`大雅`、`周頌`、`魯頌`、`商頌`。`section` 對應「周南、召南、邶風……」或「鹿鳴之什、文王之什……」等次層分類。`stanzas` 陣列中的每一項是一章；要調整分章，只需移動或拆分陣列項目，不必修改 React 元件。

## 二、編輯一篇原文

在 `client/src/data/shijing.ts` 搜尋篇名或 `id`，修改 `title`、`section` 或 `stanzas`。請勿重排既有 `id`，因為網址雜湊、收藏與本機札記均以 `id` 識別篇章。若必須新增異文，建議另加資料欄位，不要直接覆蓋底本文字而失去可追溯性。

## 三、篇旨與註釋

經文、古序、現代解讀應分層保存，不能混寫。`client/src/data/prefaces.ts` 專供具來源的《毛詩序》或其他古序；未核實的篇目保持空白，介面會顯示「尚待校訂」，不會生成或猜測內容。未來如新增現代語譯或要義，建議另建 `commentary.ts`，資料格式至少包含 `poemId`、`text`、`author`、`source`、`license` 與 `verifiedAt`。

讀者自己的「札記」保存在瀏覽器 `localStorage`，不會改動網站語料，也不會上傳伺服器。這是閱讀功能，不是共同編輯後台。

本站版次、數位校訂者、校訂者連結與最後修改日期集中於 `client/src/data/edition.ts`。任何會影響原文、篇目、分類、來源或數位呈現的實質變更，均應同步更新 `dateModified` 與 `version`。目前數位校訂者標示為 Dr.Fan；此欄位描述本站數位編排與核對責任，不宣稱取代權威古籍校勘。

## 四、主題索引

主題設定集中於 `client/src/data/topics.ts`，不需要修改 305 筆原文。傳統體例「風／雅／頌」以 `chapters` 對應既有六部分類；愛情、農事、征役、祭祀、宴飲與思歸等內容主題則以 `poemIds` 明列篇次。一篇可以同時列入多個內容主題。

```ts
{
  key: "farming",
  label: "農事",
  group: "content",
  description: "採集、耕作、牧養、農時與田野勞動",
  poemIds: [2, 8, 13, 15, 50, 112, 113, 154, 190, 209, 210, 211, 212]
}
```

新增或校訂內容主題時，應保留簡明定義、逐篇人工核對並避免重複篇次。內容分類是方便探索的**非排他編輯索引**，不是唯一篇旨，也不取代《毛詩序》、歷代箋疏或現代研究；若採用特定學者的分類體系，應另記作者、出處與版本。

## 五、資料來源與授權

原始結構化語料取自 [chinese-poetry/chinese-poetry](https://github.com/chinese-poetry/chinese-poetry/tree/master/%E8%AF%97%E7%BB%8F)，該儲存庫採 MIT License。本站以 OpenCC `s2twp` 轉為繁體，再依介面需要保留篇、部、什、章層級。古典《詩經》文本本身屬公有領域；數位轉錄、標點與資料編排仍須遵守其來源授權。

篇目總表與傳統分類另以[中文維基文庫《詩經》](https://zh.wikisource.org/zh-hant/%E8%A9%A9%E7%B6%93)交叉核對。維基文庫頁面文字依 CC BY-SA 4.0 提供；引用其校訂、編排或註釋時應保留署名與相同方式分享要求。

## 六、品質檢核

每次改動後執行：

```bash
pnpm check
pnpm build
```

若改動篇名、分部、章數、首章文字、版本或校訂資料，另執行 `pnpm generate:social`，重新產生首頁與 305 篇各自的 1200 × 630 Open Graph 圖片；建置驗證會逐一檢查圖檔、社群 Meta、修改日期與校訂者 JSON-LD。

另應核對：總篇數仍為 305；六部篇數分別為國風 160、小雅 74、大雅 31、周頌 31、魯頌 4、商頌 5；第一篇為〈關雎〉，末篇為〈殷武〉；六類內容主題至少各有一篇且全部篇次介於 1–305；搜尋、主題交叉篩選、上一首／下一首、收藏、朗讀與本機札記均可操作。

## 七、與參考站的關係

本站借鑑參考站的資訊架構概念，包括封面、抽屜式目錄、多層內容切換、前後篇導航、朗讀與本機閱讀紀錄；程式碼與視覺為獨立 React 實作。參考站儲存庫沒有 SPDX 授權聲明，且 README 明載程式碼、版面與自撰內容為站方著作，因此未直接複製其程式碼、版面數值或自撰文字。
